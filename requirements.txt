# Core pipeline
requests>=2.31
numpy>=1.26
pillow>=10.0
beautifulsoup4>=4.12
lxml>=5.0

# Models (CPU wheels: pip install --index-url https://download.pytorch.org/whl/cpu)
torch>=2.2
torchvision>=0.17
transformers>=4.40
sentencepiece>=0.2
protobuf>=4.25
safetensors>=0.4
huggingface_hub>=0.23

# MoGe-2 (vendored via tools/vendor_moge.py - not on PyPI)
utils3d>=0.1
scipy>=1.11
opencv-python-headless>=4.9

# Floor-plan OCR (also needs the tesseract binary: apt-get install tesseract-ocr)
pytesseract>=0.3.10

# Figures
matplotlib>=3.8

# Phase 1 — plan channel, geometry, shell
scikit-image>=0.22        # watershed, distance-transform peaks
shapely>=2.0              # polygon IoU, unions, shared-wall length
jsonschema>=4.20          # artifact validation at the stage boundary
referencing>=0.35         # cross-file $ref resolution in schemas/

# Optional, and NOT needed to run the Phase 1 pipeline:
#   modal, fastapi   -> modal_app/, the Phase 0 GPU validation harness
#   gsplat           -> tools/gpu_validate_standalone.py (Phase 2)
#   playwright       -> pipeline/ingest/zoopla.py browser fallback (blocked anyway)
